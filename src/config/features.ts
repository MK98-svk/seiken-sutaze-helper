// Centrálny prepínač pre sekciu Doplnky výživy.
// false = skryté v UI aj route (publikovaná verzia bez doplnkov)
// true  = viditeľné a dostupné (po explicitnom pokyne)
export const showSupplements = false;
